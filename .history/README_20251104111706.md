# Gincana - CRUD Node.js + PostgreSQL

Este é um **projeto CRUD** para a gincana.

Existem vários **erros e partes faltando** que estão com algumas partes faltando

Então todos os grupos precisam **corrigir** essas partes e **completar o código** para que o código funcione **corretamente**

Quem **terminar** primeiro recebe a pontuação

## 🚀 Tecnologias
- Node.js
- Express.js
- PostgreSQL
- HTML/CSS/JavaScript

## 📋 Funcionalidades
- Cadastro de usuários
- Listagem de usuários
- Edição de usuários
- Exclusão de usuários

## 🔧 Instalação

1. Instale as dependências:
```bash
npm install
```

2. Configure o PostgreSQL com as credenciais em `db.js`

3. Crie o banco de dados e a tabela:
```sql
CREATE TABLE usuarios (
  id SERIAL PRIMARY KEY,
  nome VARCHAR(100),
  email VARCHAR(100),
  idade INTEGER
);
```

4. Execute o servidor:
```bash
node server.js
```

5. Abra o arquivo `index.html` no navegador

## 🎯 Objetivo
Complete todas as partes quebradas do código e faça a aplicação funcionar!
